import { defineConfig } from "vite";

// https://vitejs.dev/config/
export default defineConfig({
  base: "/bond-todo/", // Custom base path for GitHub Pages deployment (matches repository bond-todo)
});
